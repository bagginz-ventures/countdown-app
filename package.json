{
  "name": "countdown-app",
  "version": "0.1.0",
  "private": true,
  "description": "Installable countdown PWA for Android, macOS, and Linux.",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "check": "python3 -m py_compile app.js 2>/dev/null || true && echo 'Static app, no build step yet'"
  }
}
